name: '🔵 GCP Action'

on:
  workflow_dispatch: {}
  push:
    branches: [main]
    paths:
      - './gcs/2d8aa135-97c0-4ab1-8443-2eafcec4055e/**'
      - '.github/workflows/gcp-action.yml'
  pull_request:
    branches: [main]
    paths:
      - './gcs/2d8aa135-97c0-4ab1-8443-2eafcec4055e/**'
      - '.github/workflows/gcp-action.yml'

jobs:
  job_id:
    name: '🆙 GCP'
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'
    timeout-minutes: 15
    environment: google-cloud
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true
    steps:
      - uses: 'actions/checkout@v3'
      - id: 'auth'
        uses: 'google-github-actions/auth@v1'
        with:
          workload_identity_provider: '${{ secrets.DIGITAL_CLOUDS_PROVIDER }}'
          service_account: '${{ secrets.DIGITAL_CLOUDS_SERVICE }}'
      #- name: '📤 digital-space'
      #  id: 'upload-digital-space'
      #  uses: 'google-github-actions/upload-cloud-storage@v0'
      #  with:
      #    path: 'gcs/63ab8820-bc9f-43aa-9045-801ac5af4bb6'
      #    destination: 'digital-space'
      #- id: 'uploaded'
      #  uses: 'digital-clouds/space/gcp-action@main'
      #  env:
      #    files: '${{ steps.upload-digital-space.outputs.uploaded }}'
      - name: '📤 gcp-space'
        id: 'upload-gcp-space'
        uses: 'google-github-actions/upload-cloud-storage@v0'
        with:
          path: 'gcs/2d8aa135-97c0-4ab1-8443-2eafcec4055e'
          destination: 'gcp-space'

      # https://github.com/google-github-actions/setup-gcloud#cloud-sdk-inputs
      #- name: 'Google Cloud SDK'
      #  id: 'setup-gcloud'
      #  uses: 'google-github-actions/setup-gcloud@v0'
      #- name: 'gcloud CLI'
      #  run: 'gcloud info'
